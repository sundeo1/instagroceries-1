@model IEnumerable<Instagroceries.Models.Test2>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Test2ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Test2ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Test2ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
